# SegNeuron 代码质量评估与润色报告

## 结论

本版本以公开提交 `659ce323` 为冻结基线，只处理代码层面的确定性缺陷、可读性、可维护性与静态质量问题。模型结构、权重键、训练与推理张量接口、损失函数映射、数据增强语义、归一化、FRMC 后处理算法及文件输入输出均未主动改变。

润色前，62 个 Python 文件在基础静态检查中共记录 551 项问题，主要包括未使用或重复导入、导入顺序、旧式类声明、可变默认参数、未定义名称、裸 `except`、无效局部变量及格式不一致。润色后，仓库配置的保守质量门禁为 0 项错误；自动安全修复 391 项，并人工处理了不能可靠自动修复的确定性问题。

## 已处理问题

- 统一 Python 文件格式和导入顺序，清理未使用、重复和通配符导入。
- 修复 `aff_util.py` 中未导入的 `check_volume`，避免相应分支运行时触发 `NameError`。
- 将裸 `except` 收窄为预期的 `TypeError`，避免吞掉中断、内存错误和无关程序缺陷。
- 将列表、数组、字典等可变默认参数改为不可变值或在函数内部创建，避免多次调用共享状态。
- 清理重复赋值、无效比较、未使用循环变量和未使用中间变量。
- 为 `model`、`loss`、`utils` 增加显式包标记，降低不同启动方式下的导入歧义。
- 新增 Ruff 配置，把适合当前 legacy 科研代码的确定性规则固化为可重复的质量门禁。
- 预训练与训练目录中复制的公共工具暂未物理合并，但增加镜像一致性测试，防止以后只修一侧。

## 行为保护

除已有的 legacy 合约测试外，本版本增加模型等价测试：从冻结提交导出的只读 reference source 加载原始 `MNet`，与润色版本使用相同随机种子初始化并执行 CPU 前向计算。监督模型与预训练模型均验证：

- `state_dict` 键名和顺序一致；
- 输出数量与通道数一致；
- 前向输出逐元素完全相等。

因此，这次格式化和代码清理没有改变网络计算结果。FRMC、损失函数及增强算法未做“看起来更合理”的数学修改，因为这类改动必须由数据集和 golden 输出单独验证。

## 仍然存在、但本轮有意保留的问题

1. `Pretrain/utils` 与 `Train_and_Inference/utils` 存在大面积复制。直接合并会改变导入和部署结构，本轮只用测试锁定同步性。
2. 增强、连通域与后处理模块函数较长，变量命名偏论文实现风格。进一步拆分可能影响随机数调用顺序或边界行为，需要 golden 数据后再做。
3. 部分兼容参数、旧式 `%` 格式化、断言式内部校验和全局随机数接口仍保留。它们影响代码风格，但不是当前确定性缺陷。
4. 目前的 golden 保护覆盖配置、采样边界、入口引用、共享工具一致性及模型数值等价；尚缺真实 checkpoint、真实体数据和 FRMC 输出的端到端 golden 样本。
5. 外部科学计算依赖较重，完整训练/推理仍取决于 CUDA、数据集、checkpoint 及 `elf` 等运行环境，轻量测试不替代实际数据验收。

## 建议的后续顺序

先固定一个小型匿名化体数据、checkpoint 及完整输出（affinity、实例标签、指标和日志摘要），再处理公共工具合并、长函数拆分、配置数据类与 FRMC 数值边界。任何会改变标签编号、连通性、阈值、随机序列或浮点结果的修改，都应作为独立变更，并与 golden 输出进行显式差异审查。

## 本地验证

```bash
python -m ruff check .
python -B -m unittest discover -s tests -v
```

当前结果：静态检查通过，8 项测试全部通过。

## torchc 兼容性实测

已在远端 `torchc` 环境完成验证：Python 3.8.10、PyTorch 1.9.0+cu102，CUDA 可用。8 项合约及模型等价测试全部通过；监督 `MNet` 与预训练 `MNet` 均使用 `(1, 1, 8, 32, 32)` 输入完成 CUDA 前向，输出通道保持为 `(3, 1)` 与 `(1, 1)`。该结果证明代码可在目标旧环境启动并执行模型计算，但不等同于使用真实数据和 checkpoint 的完整训练、推理及 FRMC 验收。
